import * as React from "react"; import { cn } from "@/lib/utils";
export const Select = React.forwardRef<HTMLSelectElement, React.SelectHTMLAttributes<HTMLSelectElement>>(({ className, children, ...p }, ref) => <select className={cn("flex h-11 w-full rounded-ios border border-separator-opaque bg-bg-primary px-4 py-2 text-sm text-label-primary shadow-sm transition-colors duration-120 focus-visible:outline-none focus-visible:border-ios-blue focus-visible:ring-1 focus-visible:ring-ios-blue-border disabled:cursor-not-allowed disabled:opacity-50", className)} ref={ref} {...p}>{children}</select>); Select.displayName = "Select";
