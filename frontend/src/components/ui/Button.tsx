import { Slot } from "@radix-ui/react-slot"; import { cva, type VariantProps } from "class-variance-authority"; import * as React from "react"; import { cn } from "@/lib/utils";
const v = cva("inline-flex items-center justify-center rounded-ios font-medium transition-colors duration-120 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ios-blue disabled:pointer-events-none disabled:opacity-50 active:scale-[0.98]", { variants: { variant: { default: "bg-ios-blue text-white hover:bg-ios-blue-hover", secondary: "bg-fill-secondary text-label-primary hover:bg-fill-primary", outline: "border border-separator-opaque bg-transparent hover:bg-fill-quaternary", ghost: "hover:bg-fill-quaternary text-label-primary", destructive: "bg-red-500 text-white hover:bg-red-600" }, size: { default: "h-11 px-4 py-2 text-sm", sm: "h-9 px-3 text-xs", lg: "h-12 px-6 text-base", icon: "h-11 w-11" } }, defaultVariants: { variant: "default", size: "default" } });
export interface P extends React.ButtonHTMLAttributes<HTMLButtonElement>, VariantProps<typeof v> { asChild?: boolean; }
export const Button = React.forwardRef<HTMLButtonElement, P>(({ className, variant, size, asChild = false, ...p }, ref) => { const Comp = asChild ? Slot : "button"; return <Comp className={cn(v({ variant, size, className }))} ref={ref} {...p} />; }); Button.displayName = "Button";
