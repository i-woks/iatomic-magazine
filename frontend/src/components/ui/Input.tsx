import * as React from "react"; import { cn } from "@/lib/utils";
export const Input = React.forwardRef<HTMLInputElement, React.InputHTMLAttributes<HTMLInputElement>>(({ className, type, ...p }, ref) => <input type={type} className={cn("flex h-11 w-full rounded-ios border border-separator-opaque bg-bg-primary px-4 py-2 text-sm text-label-primary shadow-sm transition-colors duration-120 placeholder:text-label-quaternary focus-visible:outline-none focus-visible:border-ios-blue focus-visible:ring-1 focus-visible:ring-ios-blue-border disabled:cursor-not-allowed disabled:opacity-50", className)} ref={ref} {...p} />); Input.displayName = "Input";
